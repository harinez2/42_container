#include "../src/map.hpp"
// #include "test_vector.hpp"

#include <map>

class MapTest : public ::testing::Test {
 public:
  MapTest() {
    // vft_.push_back(42);
    // vft_.push_back(1);
    // vft_.push_back(0);
    // vft_.push_back(-42);
    // vft_.push_back(4242);
    
    // vstd_.push_back(42);
    // vstd_.push_back(1);
    // vstd_.push_back(0);
    // vstd_.push_back(-42);
    // vstd_.push_back(4242);
  }
  ~MapTest() {
  }

 protected:
//   ft::map<int> mft_;
//   std::map<int> mstd_;
};

//------------------------------------------------------
// constructor (int)

TEST_F(MapTest, int_constructor_empty) {
//   ft::vector<int> vft;
//   std::vector<int> vstd;
//   EXPECT_EQ(vft.size(), 0);
//   EXPECT_TRUE(vft.empty());
//   compare_with_std_vector(vft, vstd);
}
